#include <stdio.h>

int main() {
    int arr[5] = {1, 2, 3, 4, 5};
    int key = 4;

    for (int i = 0; i < 5; i++) {
        if (arr[i] == key) {
            printf("Found at index %d", i);
            return 0;
        }
    }

    printf("Not found");
    return 0;
}
